export const fodrCurrency = (value: string) => Number(BigInt(value)) / Math.pow(10, 12);
export const warcCurrency = (value: string) => Number(BigInt(value)) / Math.pow(10, 12);
export const avaxCurrency = (value: string) => Number(BigInt(value)) / Math.pow(10, 12);
